import React from 'react'

interface Icon1Props {
  className?: string
  selected?: boolean
}

const CrawlerLinks= ({ pageProps }) => {
  console.log(pageProps);
  
  return (
    <div style={{display: "none"}}>
      
    </div>
  )
}

export default CrawlerLinks
