import { useContext } from 'react'
import MenuContext from '@/context/menu/menuContext'

const Menu = () => {
  const contactContext = useContext(MenuContext)

  return <div></div>
}

export default Menu
